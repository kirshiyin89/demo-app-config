# k8s/green/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: my-app-green
spec:
  selector:
    app: my-app
    color: green
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
